apiVersion: v1
kind: Secret
metadata:
  name: github-pat
  namespace: demo
  labels:
    app.kubernetes.io/part-of: observability-demo
  annotations:
    tekton.dev/git-0: "https://github.com"
type: Opaque
stringData:
  # Replace with a GitHub Personal Access Token (classic or fine-grained).
  # Classic PAT: enable the "repo" scope.
  # Fine-grained PAT: grant this repository Contents (Read and write)
  #   and Pull requests (Read and write). Authorize SSO if the org requires it.
  username: mike4263
  token: REPLACE_WITH_YOUR_GITHUB_PAT
